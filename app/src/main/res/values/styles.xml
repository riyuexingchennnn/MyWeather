<resources>
    <!-- 自定义Toolbar标题字体样式 -->
    <style name="ToolbarTitleStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:fontFamily">@font/custom_font</item>  <!-- 设置字体 -->
        <item name="android:textSize">16dp</item>  <!-- 设置字体大小 -->
        <item name="android:textColorPrimary">#FFFFFF</item> <!-- 设置标题文字颜色 -->
    </style>
</resources>
